[api-backend]
transport = "api"
host = "{{ icingaweb2_api_transport.host }}"
port = "{{ icingaweb2_api_transport.port }}"
username = "{{ icingaweb2_api_transport.user }}"
password = "{{ icingaweb2_api_transport.password }}"
